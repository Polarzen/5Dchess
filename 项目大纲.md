# 5D Chess with Multiverse Time Travel — 项目大纲

> 计算机科学与技术本科毕业论文 | Python + MySQL | 单人答辩

---

## 一、项目概述

### 1.1 选题背景

复刻 Steam 游戏 *"5D Chess with Multiverse Time Travel"*，在传统二维国际象棋基础上引入**时间**和**平行时间线**两个额外维度，形成五维棋类系统。棋子可以向过去移动，产生分支时间线，跨时间线的棋子移动带来全新的策略深度。

### 1.2 核心目标

| 目标 | 说明 |
|------|------|
| 完整复刻五维规则 | X轴(列) + Y轴(行) + 时间 + 平行时间线 + 跨时间线移动 |
| 三模式系统 | 真人对弈(PvP) / 人机对弈(PvE) / 棋谱回放(Replay) |
| 答辩可用 | Replay 模式作为答辩核心，展示时间线树可视化与棋谱分析 |

### 1.3 五维定义

```
维度1: X轴 — 棋盘列 (a-h, 8列)
维度2: Y轴 — 棋盘行 (1-8, 8行)
维度3: 时间 — 同一时间线内前后移动 (回合推进)
维度4: 平行时间线 — 向过去走子时产生的新分支
维度5: 跨时间线移动 — 棋子在不同时间线之间跳跃
```

**核心机制：** 棋子在当前时间线移动到过去的某一回合时，会创建一条新的平行时间线，该时间线从目标时间点分叉，包含移动的棋子。所有时间线同步推进回合。

**胜负条件：** 任意王在任意时间线的任意时间点被将死，即判负。

---

## 二、技术栈

### 2.1 语言与平台

| 层级 | 技术 | 说明 |
|------|------|------|
| 核心语言 | Python 3.10+ | 全栈统一语言 |
| 数据库 | MySQL 8.0 | 持久化游戏数据、棋谱、分析结果 |
| 版本控制 | Git | 源码管理 |

### 2.2 GUI 框架（待决策）

| 方案 | 优势 | 劣势 |
|------|------|------|
| **Pygame** | 纯Python，部署简单，离线可用 | 组件简陋，UI开发效率低 |
| **Web (Flask + HTML/CSS/JS)** | UI丰富，答辩展示效果好，可远程演示 | 需要前后端分离，技术栈更重 |

> **建议：** 若时间充裕选 Web，答辩可做成在线演示；若追求快速出原型选 Pygame。

### 2.3 时间线树可视化（待决策）

| 方案 | 适用场景 |
|------|----------|
| **ECharts** | Web 方案首选，树图/力导向图丰富 |
| **D3.js** | 高度自定义，开发成本高 |
| **Canvas 自绘** | Pygame 方案配套 |

### 2.4 AI 引擎

| 难度 | 算法 | 搜索深度 |
|------|------|----------|
| 简单 | 随机合法走子 | — |
| 中等 | Alpha-Beta 剪枝 + 评估函数 | 2-3层 |
| 困难 | Alpha-Beta 深搜索 + 开局库 / MCTS | 4-5层 |

### 2.5 辅助库

| 库 | 用途 |
|----|------|
| `numpy` | 棋盘状态矩阵运算 |
| `pygame` / `flask` | GUI |
| `mysql-connector-python` / `SQLAlchemy` | 数据库连接 |
| `json` | `.5dpgn` 棋谱文件解析 |
| `pytest` | 单元测试 |

---

## 三、系统架构

### 3.1 三层架构图

```
┌─────────────────────────────────────────────────────┐
│                    表现层 (GUI)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 棋盘视图  │  │时间线树   │  │ 控制面板/UI组件  │   │
│  │(8×8+多线) │  │(可视化)   │  │(按钮/信息/状态)  │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
├─────────────────────────────────────────────────────┤
│                    逻辑层 (Engine)                    │
│  ┌──────────────────────────────────────────────┐   │
│  │              5D 核心引擎 (5DEngine)            │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌───────┐  │   │
│  │  │走子生成│ │合法性  │ │将军/   │ │分支   │  │   │
│  │  │        │ │校验    │ │将杀判定│ │管理   │  │   │
│  │  └────────┘ └────────┘ └────────┘ └───────┘  │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ PvP 模式 │  │ PvE 模式 │  │  Replay 模式     │   │
│  │(热座对战)│  │(AI对手)  │  │(棋谱回放+分析)   │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
├─────────────────────────────────────────────────────┤
│                    数据层 (Data)                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  MySQL   │  │ .5dpgn   │  │  异步写入队列     │   │
│  │  数据库   │  │ 棋谱文件  │  │  (不阻塞主循环)  │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 3.2 数据流

```
用户操作 → GUI 事件 → 模式控制器(PvP/PvE/Replay)
    → 5DEngine 处理(走子合法性/分支/胜负)
    → 状态更新 → GUI 刷新
    → 异步写入 MySQL + .5dpgn 文件
```

### 3.3 核心类设计

```python
# 5D 核心引擎
class FiveDEngine:
    timelines: dict[int, Timeline]     # 所有时间线
    active_timeline_id: int            # 当前活跃时间线
    current_turn: int                  # 当前全局回合
    game_state: GameState              # 游戏状态

class Timeline:
    timeline_id: int
    parent_id: int | None              # 父时间线ID
    branch_point: tuple                # 分支点 (回合, 走子)
    positions: list[Position]          # 每个时间点的棋盘状态
    is_active: bool

class Position:
    board: ndarray[8, 8]               # 8×8棋盘矩阵
    turn: ChessColor                   # 当前走子方
    timeline_id: int
    time_point: int                    # 时间点(回合)

class Move:
    piece: Piece
    from: tuple  # (timeline_id, x, y, time_point)
    to: tuple    # (timeline_id, x, y, time_point)
    is_branching: bool                 # 是否产生新时间线

# 模式
class GameMode(ABC):
    engine: FiveDEngine

class PvPMode(GameMode): ...
class PvEMode(GameMode):
    ai: AIPlayer
class ReplayMode(GameMode):
    moves: list[Move]
    current_index: int
```

---

## 四、五维规则引擎详细设计

### 4.1 走子生成

传统国际象棋走子 + 时间维度扩展：

```
传统走子 (空间)
├── 兵: 前进1格/2格(初始), 斜吃
├── 马: L形跳跃
├── 象: 斜线
├── 车: 直线
├── 后: 直线+斜线
└── 王: 周围1格 + 王车易位

时间维度扩展 (五维特有)
├── 时间前进: 棋子向"未来"移动 (同时间线, 回合+1)
├── 时间后退: 棋子向"过去"移动 → 产生新分支时间线
└── 跨时间线: 棋子在不同时间线之间跳跃
```

### 4.2 分支机制

```
触发条件: 棋子移动的目标时间点 < 当前回合
处理流程:
  1. 复制目标时间点的棋盘状态
  2. 创建新 Timeline (parent = 当前时间线)
  3. 在新时间线的目标时间点放置棋子
  4. 新时间线从该时间点开始独立推进
  5. 原时间线不受影响
```

### 4.3 将军/将杀判定

```
将军: 王所在位置被对方棋子攻击
将杀: 将军 + 所有合法走子均无法解除将军

五维特殊: 需检查所有时间线中所有时间点的王
  任一王被将死 → 游戏结束
```

### 4.4 合法走子过滤

```
1. 生成所有伪合法走子
2. 过滤: 走子后己方王不能被将军
3. 过滤: 不能产生时间悖论(可选规则)
4. 返回合法走子列表
```

---

## 五、数据库设计 (MySQL)

### 5.1 ER 图概要

```
games ──1:N──> timelines ──1:N──> positions
  │                                    │
  └──1:N──> moves ─────────────────────┘
```

### 5.2 核心表

#### games — 游戏记录

| 字段 | 类型 | 说明 |
|------|------|------|
| game_id | INT PK AUTO_INCREMENT | 游戏ID |
| mode | ENUM('pvp','pve','replay') | 模式 |
| player_white | VARCHAR(64) | 白方(玩家名/AI) |
| player_black | VARCHAR(64) | 黑方 |
| ai_difficulty | ENUM('easy','medium','hard') NULL | AI难度 |
| result | ENUM('white_win','black_win','draw','ongoing') | 结果 |
| start_time | DATETIME | 开始时间 |
| end_time | DATETIME NULL | 结束时间 |
| total_moves | INT DEFAULT 0 | 总走子数 |
| total_timelines | INT DEFAULT 1 | 时间线总数 |

#### timelines — 时间线

| 字段 | 类型 | 说明 |
|------|------|------|
| timeline_id | INT PK AUTO_INCREMENT | 时间线ID |
| game_id | INT FK → games | 所属游戏 |
| parent_id | INT FK → timelines NULL | 父时间线 |
| branch_move_id | INT FK → moves NULL | 触发分支的走子 |
| branch_turn | INT NULL | 分支发生的回合 |
| created_at | DATETIME | 创建时间 |

#### moves — 走子记录

| 字段 | 类型 | 说明 |
|------|------|------|
| move_id | INT PK AUTO_INCREMENT | 走子ID |
| game_id | INT FK → games | 所属游戏 |
| timeline_id | INT FK → timelines | 发生时间线 |
| turn_number | INT | 当前回合 |
| piece_type | ENUM('K','Q','R','B','N','P') | 棋子类型 |
| piece_color | ENUM('white','black') | 棋子颜色 |
| from_timeline_id | INT | 来源时间线 |
| from_x | TINYINT | 来源列(0-7) |
| from_y | TINYINT | 来源行(0-7) |
| from_time | INT | 来源时间点 |
| to_timeline_id | INT | 目标时间线 |
| to_x | TINYINT | 目标列 |
| to_y | TINYINT | 目标行 |
| to_time | INT | 目标时间点 |
| is_branching | BOOLEAN DEFAULT FALSE | 是否产生分支 |
| new_timeline_id | INT FK → timelines NULL | 新创建的时间线 |
| notation | VARCHAR(64) | 棋谱记法 |
| created_at | DATETIME | 走子时间 |

#### positions — 棋盘快照

| 字段 | 类型 | 说明 |
|------|------|------|
| position_id | INT PK AUTO_INCREMENT | 快照ID |
| timeline_id | INT FK → timelines | 所属时间线 |
| turn_number | INT | 回合号 |
| time_point | INT | 时间点 |
| board_fen | VARCHAR(256) | 扩展FEN(含时间信息) |
| board_json | JSON | 完整棋盘状态 |
| active_color | ENUM('white','black') | 当前走子方 |
| is_check | BOOLEAN | 是否将军 |
| is_checkmate | BOOLEAN | 是否将杀 |

### 5.3 扩展表

#### game_stats — 对局统计

| 字段 | 类型 | 说明 |
|------|------|------|
| stat_id | INT PK | 统计ID |
| game_id | INT FK | 所属游戏 |
| avg_branch_depth | FLOAT | 平均分支深度 |
| max_timelines | INT | 最大同时活跃时间线 |
| white_time_travels | INT | 白方时间旅行次数 |
| black_time_travels | INT | 黑方时间旅行次数 |

#### openings — 开局库

| 字段 | 类型 | 说明 |
|------|------|------|
| opening_id | INT PK | 开局ID |
| name | VARCHAR(128) | 开局名称 |
| moves_sequence | JSON | 走子序列 |
| win_rate_white | FLOAT | 白方胜率 |
| total_games | INT | 总对局数 |

### 5.4 异步写入策略

```python
import threading
from queue import Queue

class AsyncDBWriter:
    """异步写入数据库，不阻塞游戏主循环"""
    _queue: Queue = Queue()
    _worker: threading.Thread

    def write_move(self, move_data: dict):
        self._queue.put(('move', move_data))

    def write_position(self, pos_data: dict):
        self._queue.put(('position', pos_data))

    def _run(self):
        while True:
            op, data = self._queue.get()
            if op == 'move':
                self._insert_move(data)
            elif op == 'position':
                self._insert_position(data)
```

---

## 六、三模式详细设计

### 6.1 PvP — 真人对弈

```
流程:
  1. 白方走子 → 引擎处理 → 刷新显示
  2. 黑方走子 → 引擎处理 → 刷新显示
  3. 重复直到将杀/认输/和棋

特点:
  - 同屏热座 (同一设备交替操作)
  - 完整走子合法性校验
  - 实时将军/将杀提示
  - 自动记录棋谱
```

### 6.2 PvE — 人机对弈

```
流程:
  1. 玩家走子 → 引擎处理
  2. AI计算(异步) → 自动走子
  3. 重复直到终局

AI 三档:
  easy:    随机选合法走子
  medium:  Alpha-Beta 搜索, 深度2-3, 基础评估函数
  hard:    Alpha-Beta 搜索, 深度4-5, 开局库 + 进阶评估

评估函数要素:
  - 子力价值 (K=∞, Q=9, R=5, B=3, N=3, P=1)
  - 位置价值表 (positional tables)
  - 时间线控制 (控制更多时间线加分)
  - 王的安全 (跨时间线检查)
```

### 6.3 Replay — 棋谱回放（答辩核心）

```
功能:
  1. 逐步回放: 前一步/后一步
  2. 时间线树可视化: 树状图展示所有分支
  3. 时间线切换: 点击分支节点切换查看
  4. 快进/快退: 拖动时间轴
  5. 整体预览: 俯瞰所有时间线的最终状态
  6. 统计面板: 走子数、分支数、时间旅行次数等

界面布局:
  ┌──────────────────────┬──────────────┐
  │                      │              │
  │   棋盘视图(主区域)     │  时间线树    │
  │   (含当前时间线信息)   │  (树状可视化) │
  │                      │              │
  ├──────────────────────┴──────────────┤
  │  控制栏: ◀◀ ◀ ▶ ▶▶  │ 回合: 12/45  │
  │  统计信息: 时间线×3, 分支×5, ...     │
  └─────────────────────────────────────┘
```

---

## 七、.5dpgn 棋谱文件格式

### 7.1 格式设计（扩展 PGN）

```
[Game "5D Chess"]
[Mode "pvp"]
[Date "2026.08.23"]
[White "Player1"]
[Black "Player2"]
[Result "white_win"]
[TotalTimelines "3"]
[TotalMoves "45"]

1. (T0) e2-e4 : e7-e5
2. (T0) Ng1-f3 : Nb8-c6
3. (T0) Bf1-c4 : Bf8-c5
   (T1) [branch from T0:2w] Bc4→(T0,t=-1) → NEW TIMELINE T1
4. (T0) ...
   (T1) ...
```

### 7.2 解析器

```python
class FiveDPGN:
    @staticmethod
    def load(filepath: str) -> list[Move]:
        """解析 .5dpgn 文件为走子序列"""
        ...

    @staticmethod
    def save(filepath: str, game: Game):
        """导出游戏为 .5dpgn 文件"""
        ...
```

---

## 八、项目文件结构

```
5D_Chess/
├── README.md
├── requirements.txt
├── docs/
│   ├── 论文大纲.md
│   ├── 五维规则说明.md
│   └── 答辩PPT大纲.md
├── src/
│   ├── main.py                    # 入口
│   ├── config.py                  # 配置
│   ├── engine/
│   │   ├── __init__.py
│   │   ├── board.py               # 棋盘表示 Position
│   │   ├── piece.py               # 棋子定义 Piece
│   │   ├── move_generator.py      # 走子生成
│   │   ├── move_validator.py      # 合法性校验
│   │   ├── timeline.py            # 时间线管理 Timeline
│   │   ├── engine.py              # 5D核心引擎 FiveDEngine
│   │   └── rules.py               # 将军/将杀/和棋判定
│   ├── modes/
│   │   ├── __init__.py
│   │   ├── base.py                # GameMode 抽象基类
│   │   ├── pvp.py                 # PvP 模式
│   │   ├── pve.py                 # PvE 模式
│   │   └── replay.py              # Replay 模式
│   ├── ai/
│   │   ├── __init__.py
│   │   ├── base.py                # AIPlayer 基类
│   │   ├── random_ai.py           # 简单AI
│   │   ├── alpha_beta.py          # 中等AI
│   │   ├── hard_ai.py             # 困难AI
│   │   ├── evaluator.py           # 评估函数
│   │   └── opening_book.py        # 开局库
│   ├── gui/
│   │   ├── __init__.py
│   │   ├── app.py                 # GUI 主循环
│   │   ├── board_view.py          # 棋盘视图
│   │   ├── timeline_tree.py       # 时间线树可视化
│   │   ├── control_panel.py       # 控制面板
│   │   └── assets/                # 图片/字体资源
│   ├── data/
│   │   ├── __init__.py
│   │   ├── db.py                  # MySQL 连接管理
│   │   ├── models.py              # ORM 模型
│   │   ├── async_writer.py        # 异步写入
│   │   ├── pgn_parser.py          # .5dpgn 解析/导出
│   │   └── migrations/            # 数据库迁移脚本
│   └── utils/
│       ├── __init__.py
│       ├── constants.py           # 常量
│       └── logger.py              # 日志
├── tests/
│   ├── test_engine.py
│   ├── test_move_generator.py
│   ├── test_timeline.py
│   ├── test_ai.py
│   └── test_db.py
├── data/
│   ├── openings.json              # 开局库数据
│   └── replays/                   # 示例棋谱
└── sql/
    └── schema.sql                 # 数据库建表脚本
```

---

## 九、开发路线图

### 阶段一：基础引擎 (预计 2-3 个月)

| 里程碑 | 内容 |
|--------|------|
| M1.1 | 传统国际象棋引擎：棋盘表示、走子生成、合法性校验 |
| M1.2 | 将军/将杀/和棋判定 |
| M1.3 | 单元测试覆盖核心引擎 |

### 阶段二：五维扩展 (预计 2-3 个月)

| 里程碑 | 内容 |
|--------|------|
| M2.1 | 时间线数据结构实现 |
| M2.2 | 时间移动(前进/后退)走子生成 |
| M2.3 | 分支机制：向过去走子 → 新时间线 |
| M2.4 | 跨时间线移动 |
| M2.5 | 五维将军/将杀判定 |

### 阶段三：数据库与持久化 (预计 1-2 个月)

| 里程碑 | 内容 |
|--------|------|
| M3.1 | MySQL 建表与 ORM 模型 |
| M3.2 | 异步写入机制 |
| M3.3 | .5dpgn 棋谱格式定义与解析器 |
| M3.4 | 棋谱导入/导出 |

### 阶段四：GUI (预计 2-3 个月)

| 里程碑 | 内容 |
|--------|------|
| M4.1 | 棋盘视图(8×8 + 多时间线) |
| M4.2 | 时间线树可视化 |
| M4.3 | 控制面板与UI组件 |
| M4.4 | PvP 模式界面接入 |

### 阶段五：AI (预计 2-3 个月)

| 里程碑 | 内容 |
|--------|------|
| M5.1 | 随机AI (easy) |
| M5.2 | Alpha-Beta + 评估函数 (medium) |
| M5.3 | 深搜索 + 开局库 (hard) |
| M5.4 | AI 对战测试与调优 |

### 阶段六：Replay 模式 (预计 1-2 个月)

| 里程碑 | 内容 |
|--------|------|
| M6.1 | 逐步回放控制 |
| M6.2 | 时间线树交互 |
| M6.3 | 统计面板与分析 |
| M6.4 | 棋谱录制与回放完整性测试 |

### 阶段七：论文与答辩 (预计 2-3 个月)

| 里程碑 | 内容 |
|--------|------|
| M7.1 | 论文初稿 |
| M7.2 | 系统测试与Bug修复 |
| M7.3 | 答辩PPT |
| M7.4 | 模拟答辩 |

---

## 十、关键技术难点与应对

| 难点 | 风险 | 应对策略 |
|------|------|----------|
| 分支爆炸(时间线过多) | 性能/存储 | 限制最大活跃时间线数，惰性加载 |
| 五维将杀判定 | 正确性 | 充分单元测试，构造标准残局验证 |
| AI 在五维空间搜索 | 搜索空间巨大 | 剪枝策略，限制跨时间线搜索深度 |
| 时间线树可视化 | 用户体验 | 参考成熟树图方案，不重复造轮子 |
| 异步写入数据一致性 | 数据丢失 | 先写本地日志，再异步同步到MySQL |

---

## 十一、答辩策略

### 11.1 演示重点

1. **PvP 快速演示** — 展示基本玩法，刻意制造一次时间旅行分支
2. **PvE 对战** — 人与 AI 对战，展示 AI 也会使用时间旅行
3. **Replay 深度展示** — 这是核心：
   - 加载一盘有丰富分支的棋谱
   - 逐步回放，展示时间线树的生长
   - 切换不同时间线查看
   - 展示统计分析面板

### 11.2 预备棋谱

提前准备 3-5 盘高质量对局棋谱，确保：
- 至少 2 盘有多次时间旅行分支
- 至少 1 盘有多条活跃时间线
- 至少 1 盘展示了跨时间线移动的战术

---

*最后更新: 2026-08-23*